#include <stdio.h>

int main(){
int x,y;

int a = (x*x*2) - (5*x) +1;
int b = (y*y*2);


printf("Type the value of x in p(x) : \n ");
scanf("%d",&x);
printf("Type the value of y in p(y) : \n ");
scanf("%d",&y);


    
    return 0;
}