#include <stdio.h>

int main()
{
    char str[20];
    int length;
    printf("Enter the string : \n");
    scanf("%s", str);
    length = strlen(str);
    printf("Lenght of the string is : %d\n, length");
}